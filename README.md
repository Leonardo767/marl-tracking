# Muti-Agent RL Tracking

Installation:
* `git clone git@github.com:Leonardo767/marl-tracking.git`
* As a separate cloned repo, `git clone https://github.com/openai/multiagent-particle-envs.git`
* `cd` into the repo
* `pip install -e .`
